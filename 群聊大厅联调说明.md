# 群聊大厅与入群审核接口文档

更新时间：2026-04-28

## 本次后端改动

- `chat_group` 新增 `isPublic`、`joinRequiresApproval`，默认分别为 `1`、`0`。
- 新增入群申请表 `chat_group_join_request`，支持 `PENDING -> APPROVED/REJECTED`。
- 群聊大厅新增公开群分页接口，并返回 `onlineCount`。
- `POST /api/group/join` 改为使用当前登录用户，不再信任请求体里的 `userId`。
- 群组创建接口同样使用当前登录用户作为群主，`creatorId` 请求字段会被忽略。
- 群成员写入改为 upsert，用户退群后再次加入不会因唯一索引失败。

## 前端大厅字段映射

截图里的群聊大厅按以下规则接入：

- 保留：搜索框、创建群组按钮、群列表。
- 去掉：分类筛选、最近活跃筛选、标签、右侧操作列。
- 行点击：点击整行进入群聊或触发加入/申请入群逻辑。
- “活跃度”改为在线人数：展示 `onlineCount`。

大厅列表字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `groupId` | Long | 群号 |
| `name` | String | 群名称 |
| `avatarUrl` | String | 群头像 |
| `description` | String | 群简介 |
| `memberCount` | Integer | 当前成员数 |
| `maxMembers` | Integer | 成员上限 |
| `onlineCount` | Integer | 当前 WebSocket 在线人数 |
| `isMember` | Boolean | 当前用户是否已在群内 |
| `joinRequestStatus` | String | 当前用户待审核申请状态，通常为 `PENDING` 或 `null` |
| `joinRequiresApproval` | Integer | `1` 需要审核，`0` 直接加入 |

## 接口清单

### 1. 查询群聊大厅

`GET /api/group/hall?page=1&size=10&keyword=AI`

只返回 `isPublic=1` 且未删除的群组。

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": {
    "total": 12,
    "records": [
      {
        "groupId": 123456789,
        "name": "AI创作交流群",
        "avatarUrl": "https://example.com/group.jpg",
        "description": "分享AI绘画、写作与视频创作经验",
        "memberCount": 1245,
        "maxMembers": 5000,
        "onlineCount": 36,
        "isMember": false,
        "joinRequestStatus": null,
        "joinRequiresApproval": 0
      }
    ]
  }
}
```

### 2. 创建群组

`POST /api/group/create`

请求体：

```json
{
  "name": "AI创作交流群",
  "characterId": 920000000001,
  "description": "分享AI绘画、写作与视频创作经验",
  "isPublic": 1,
  "joinRequiresApproval": 0
}
```

说明：

- `creatorId` 不需要传，后端使用当前登录用户。
- `isPublic` 不传默认 `1`。
- `joinRequiresApproval` 不传默认 `0`。

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": 123456789
}
```

### 3. 设置是否展示到群聊大厅

`POST /api/group/{groupId}/public`

仅群内 `OWNER` 或 `ADMIN` 可操作。

请求体：

```json
{
  "isPublic": true
}
```

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": 1
}
```

### 4. 设置入群是否需要审核

`POST /api/group/{groupId}/join-approval`

仅群内 `OWNER` 或 `ADMIN` 可操作。

请求体：

```json
{
  "joinRequiresApproval": true
}
```

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": 1
}
```

### 5. 加入群组 / 提交入群申请

`POST /api/group/join`

请求体：

```json
{
  "groupId": 123456789,
  "nickname": "我的群昵称"
}
```

说明：

- `userId` 不需要传，后端使用当前登录用户。
- 若群组关闭审核，接口直接写入群成员。
- 若群组开启审核，接口创建 `PENDING` 入群申请，不写入群成员。
- 重复提交待审核申请时返回原申请，不新增重复记录。

直接加入响应：

```json
{
  "code": 1,
  "msg": null,
  "data": {
    "status": "JOINED",
    "groupId": 123456789,
    "requestId": null,
    "message": "已加入群聊"
  }
}
```

待审核响应：

```json
{
  "code": 1,
  "msg": null,
  "data": {
    "status": "PENDING",
    "groupId": 123456789,
    "requestId": 88,
    "message": "入群申请已提交，等待管理员审核"
  }
}
```

已在群内响应：

```json
{
  "code": 1,
  "msg": null,
  "data": {
    "status": "ALREADY_MEMBER",
    "groupId": 123456789,
    "requestId": null,
    "message": "已在群聊中"
  }
}
```

### 6. 查询入群申请

`GET /api/group/{groupId}/join-requests?status=PENDING`

仅群内 `OWNER` 或 `ADMIN` 可操作。

`status` 可选：`PENDING`、`APPROVED`、`REJECTED`，默认 `PENDING`。

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": [
    {
      "requestId": 88,
      "groupId": 123456789,
      "userId": 10001,
      "nickname": "我的群昵称",
      "username": "test_user",
      "avatarUrl": "https://example.com/avatar.png",
      "status": "PENDING",
      "reviewerId": null,
      "reviewTime": null,
      "rejectReason": null,
      "createTime": "2026-04-28T23:30:00",
      "updateTime": "2026-04-28T23:30:00"
    }
  ]
}
```

### 7. 通过入群申请

`POST /api/group/join-requests/{requestId}/approve`

仅群内 `OWNER` 或 `ADMIN` 可操作。通过后写入群成员。

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": null
}
```

### 8. 拒绝入群申请

`POST /api/group/join-requests/{requestId}/reject`

仅群内 `OWNER` 或 `ADMIN` 可操作。

请求体：

```json
{
  "reason": "暂不符合群聊主题"
}
```

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": null
}
```

### 9. 查询群组在线人数

`GET /api/group/{groupId}/online-count`

响应：

```json
{
  "code": 1,
  "msg": null,
  "data": 36
}
```

## 状态流转

```text
PENDING -> APPROVED
PENDING -> REJECTED
```

- `APPROVED` 后用户成为群成员。
- `REJECTED` 后用户不会成为群成员，可重新提交申请。
- 非待审核状态再次审批会返回失败消息：`入群申请状态已变化`。

## 错误说明

```json
{
  "code": 3002,
  "msg": "无权限操作入群申请",
  "data": null
}
```

```json
{
  "code": 3006,
  "msg": "群组人数已满",
  "data": null
}
```

```json
{
  "code": 3007,
  "msg": "入群申请不存在",
  "data": null
}
```
