<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuntian.chat_app.mapper.userMapper.UserMapper">
    <insert id="insert" parameterType="com.yuntian.chat_app.entity.User">
        insert into app_user (id, username, password, create_time, update_time)
        values (#{id},#{username}, #{password}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        update app_user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            update_time = CURRENT_TIMESTAMP
        </set>
        where id = #{id}
    </update>
</mapper>
