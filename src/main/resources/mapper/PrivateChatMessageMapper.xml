<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuntian.chat_app.mapper.userMapper.PrivateChatMessageMapper">

    <resultMap id="BaseResultMap" type="com.yuntian.chat_app.entity.PrivateChatMessage">
        <id column="id" property="id"/>
        <result column="memory_id" property="memoryId"/>
        <result column="user_id" property="userId"/>
        <result column="character_id" property="characterId"/>
        <result column="sender_type" property="senderType"/>
        <result column="content" property="content"/>
        <result column="image_url" property="imageUrl"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO private_chat_message (
            memory_id, user_id, character_id, sender_type,
            content, image_url, create_time, is_deleted
        ) VALUES (
                     #{memoryId}, #{userId}, #{characterId}, #{senderType},
                     #{content}, #{imageUrl}, NOW(), 0
                 )
    </insert>

    <select id="selectByMemoryId" resultMap="BaseResultMap">
        SELECT * FROM private_chat_message
        WHERE memory_id = #{memoryId} AND is_deleted = 0
        ORDER BY create_time ASC  LIMIT #{offset}, #{limit}
    </select>

    <update id="deleteById">
        UPDATE private_chat_message SET is_deleted = 1 WHERE id = #{id}
    </update>

</mapper>